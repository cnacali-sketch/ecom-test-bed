fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy>=2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0
pydantic>=2.10.3
pydantic-settings==2.7.0
# Backs pydantic's EmailStr (auth schemas).
email-validator==2.2.0
celery==5.4.0
redis==5.2.1
# Auth (P1): password hashing and JWT. Both are security primitives that must
# never be hand-rolled; bcrypt is used directly rather than via passlib.
bcrypt==4.2.1
PyJWT==2.10.1
python-barcode==0.15.1
Pillow==11.0.0
textstat==0.7.4
# textstat depends on pkg_resources, which recent setuptools no longer ships.
# Pin below 81 so the optional textstat path works; app.services.description_analyzer
# falls back to a self-contained Flesch approximation if textstat is unavailable.
setuptools<81
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
